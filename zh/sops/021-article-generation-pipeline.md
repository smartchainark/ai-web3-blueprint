# 文章生成流水线：AI 驱动的多风格内容工厂

> 从原始文档到多平台发布的全自动化内容生产流水线，支持并行生成、智能配图、模板定制

**文档编号**: 021
**日期**: 2025-12-29
**标签**: `AI生成` `内容流水线` `多风格` `生图` `自动发布`

---

## 概述

本文档描述了一个完整的 AI 驱动内容生成流水线，将一篇源文档自动转换为多种风格的文案，并配合 AI 生图、模板定制和多平台发布。

### 流水线全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        文章生成流水线 (Article Generation Pipeline)            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐                                                            │
│  │  源文档输入  │  ← 原始 Markdown/文档                                       │
│  └──────┬──────┘                                                            │
│         │                                                                   │
│         ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐           │
│  │              Stage 1: 并行多风格生成                          │           │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │           │
│  │  │  技术型文案  │  │  故事型文案  │  │  产品型文案  │          │           │
│  │  │   Agent 1   │  │   Agent 2   │  │   Agent 3   │          │           │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │           │
│  └─────────┼────────────────┼────────────────┼──────────────────┘           │
│            │                │                │                              │
│            ▼                ▼                ▼                              │
│  ┌─────────────────────────────────────────────────────────────┐           │
│  │              Stage 2: 金句提取 & 图片提示词                    │           │
│  │  ┌──────────────────────────────────────────────────────┐   │           │
│  │  │  📝 金句识别  →  🎨 上下文融合  →  💡 Prompt 生成       │   │           │
│  │  └──────────────────────────────────────────────────────┘   │           │
│  └──────────────────────────────┬──────────────────────────────┘           │
│                                 │                                           │
│                                 ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐           │
│  │              Stage 3: AI 生图 (异步)                          │           │
│  │  ┌─────────────┐  ┌─────────────┐                           │           │
│  │  │   即梦 API   │  │  Banana AI  │  ← 可配置生图引擎          │           │
│  │  └──────┬──────┘  └──────┬──────┘                           │           │
│  │         └────────┬───────┘                                  │           │
│  │                  ▼                                          │           │
│  │         ┌─────────────┐                                     │           │
│  │         │  图片插入器  │  → 异步插入到文章指定位置              │           │
│  │         └─────────────┘                                     │           │
│  └──────────────────────────────┬──────────────────────────────┘           │
│                                 │                                           │
│                                 ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐           │
│  │              Stage 4: 二次加工 (模板系统)                      │           │
│  │                                                             │           │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │           │
│  │  │  风格    │  │  颜色    │  │  边框    │  │  字体    │    │           │
│  │  │  选择器  │  │  选择器  │  │  选择器  │  │  选择器  │    │           │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │           │
│  │                                                             │           │
│  │  预设主题: [ 简约白 | 科技蓝 | 暖心橙 | 专业黑 | 自定义 ]      │           │
│  └──────────────────────────────┬──────────────────────────────┘           │
│                                 │                                           │
│                                 ▼                                           │
│  ┌─────────────────────────────────────────────────────────────┐           │
│  │              Stage 5: 发布平台选择                            │           │
│  │                                                             │           │
│  │  ┌──────┐  ┌──────┐  ┌────────┐  ┌──────┐  ┌──────┐       │           │
│  │  │ 知乎  │  │ 掘金  │  │ 微信公众号│  │ 小红书 │  │ 抖音  │       │           │
│  │  └──────┘  └──────┘  └────────┘  └──────┘  └──────┘       │           │
│  │                                                             │           │
│  │  支持: 单平台发布 | 多平台并行发布 | 定时发布                   │           │
│  └─────────────────────────────────────────────────────────────┘           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 第一部分：多风格并行生成

### 1.1 三种核心风格

| 风格 | 特点 | 适用场景 | 目标读者 |
|------|------|----------|----------|
| **技术型** | 精确、专业、代码示例丰富 | 技术博客、开发者社区 | 程序员、技术爱好者 |
| **故事型** | 叙事、情感、场景化描述 | 公众号、知乎、小红书 | 普通用户、产品体验者 |
| **产品型** | 卖点突出、价值驱动、CTA明确 | 产品发布、营销文案 | 潜在用户、决策者 |

### 1.2 并行生成架构

```javascript
// 使用 Claude Task 工具并行启动 3 个子任务
async function generateMultiStyleArticles(sourceDoc) {
  const styles = ['technical', 'storytelling', 'product'];

  // 并行启动 3 个 Agent
  const tasks = styles.map(style => Task({
    subagent_type: 'general-purpose',
    prompt: `将以下文档改写为${getStyleDescription(style)}风格的文章：\n\n${sourceDoc}`,
    model: 'sonnet'
  }));

  return Promise.all(tasks);
}
```

### 1.3 风格转换提示词模板

#### 技术型 (Technical)

```markdown
## 转换指令

将原文改写为技术型文章，要求：

1. **结构清晰**：使用层级标题、编号列表
2. **代码优先**：每个概念配合代码示例
3. **精确术语**：使用行业标准术语
4. **数据支撑**：添加性能数据、对比图表
5. **操作导向**：提供可复制的命令和配置

### 金句格式
> 💡 **技术洞察**：[精炼的技术观点]

### 示例结构
- 背景问题
- 技术方案
- 代码实现
- 效果验证
- 最佳实践
```

#### 故事型 (Storytelling)

```markdown
## 转换指令

将原文改写为故事型文章，要求：

1. **场景开头**：以具体场景或问题引入
2. **人物代入**：使用"我"或典型用户视角
3. **情感连接**：描述痛点和解决后的愉悦感
4. **转折起伏**：遇到问题 → 探索方案 → 豁然开朗
5. **金句收尾**：用一句话总结核心价值

### 金句格式
> ✨ **灵感时刻**：[触动人心的感悟]

### 示例结构
- 痛点场景（引起共鸣）
- 探索过程（代入感）
- 发现方案（转折点）
- 使用体验（价值验证）
- 心得感悟（升华主题）
```

#### 产品型 (Product)

```markdown
## 转换指令

将原文改写为产品型文章，要求：

1. **价值前置**：开头直接说明能解决什么问题
2. **卖点提炼**：3-5 个核心卖点，用图标标注
3. **对比优势**：与替代方案的对比表格
4. **使用简单**：3 步上手指南
5. **行动号召**：明确的 CTA（立即试用/查看详情）

### 金句格式
> 🚀 **核心价值**：[一句话价值主张]

### 示例结构
- 价值主张（解决什么问题）
- 核心卖点（为什么选择）
- 快速上手（怎么开始）
- 成功案例（谁在用）
- 立即行动（下一步）
```

---

## 第二部分：金句提取与图片提示词

### 2.1 金句识别规则

金句是文章中最具传播力的语句，通常具有以下特征：

```yaml
金句特征:
  - 简洁有力：10-30 字
  - 观点鲜明：表达独特见解
  - 易于记忆：节奏感强、押韵或对仗
  - 情感共鸣：触动读者情绪
  - 价值明确：传递核心信息

识别标记:
  - 技术型：💡 或 > **技术洞察**
  - 故事型：✨ 或 > **灵感时刻**
  - 产品型：🚀 或 > **核心价值**
```

### 2.2 图片提示词生成策略

```javascript
function generateImagePrompt(quote, context) {
  const prompt = {
    // 基础描述
    subject: extractSubject(quote),

    // 风格参数
    style: {
      art_style: 'digital illustration, modern, clean',
      color_palette: 'vibrant, tech-inspired blues and purples',
      mood: 'innovative, professional, inspiring'
    },

    // 上下文融合
    context_elements: extractKeyElements(context),

    // 技术参数
    technical: {
      aspect_ratio: '16:9',
      quality: 'high detail, 4K',
      lighting: 'soft studio lighting'
    }
  };

  return formatPrompt(prompt);
}
```

### 2.3 提示词模板

```markdown
## 图片提示词结构

### 主体描述
[基于金句的核心概念视觉化]

### 风格定义
- 艺术风格：数字插画 / 3D渲染 / 扁平设计
- 色彩方案：科技蓝 / 温暖橙 / 简约黑白
- 情绪氛围：专业 / 温馨 / 活力 / 沉稳

### 细节补充
- 背景元素：抽象几何 / 渐变色块 / 网格线条
- 点缀元素：图标 / 光效 / 粒子效果
- 文字叠加：是否包含金句文字

### 技术参数
- 尺寸比例：16:9（文章头图） / 1:1（卡片） / 9:16（小红书）
- 质量要求：高清 / 4K
- 输出格式：PNG / WebP
```

---

## 第三部分：AI 生图集成

### 3.1 生图引擎配置

支持多个生图 API，可根据需求切换：

```yaml
image_generators:
  jimeng:
    name: 即梦 AI
    api_endpoint: https://jimeng.jianying.com/ai-tool/image
    features:
      - 中文提示词优化
      - 多种艺术风格
      - 快速生成
    pricing: 按次计费

  banana:
    name: Banana AI
    api_endpoint: https://api.banana.dev
    features:
      - Stable Diffusion 模型
      - 高度可定制
      - 批量生成
    pricing: 按秒计费

  midjourney:
    name: Midjourney
    api_endpoint: via Discord Bot
    features:
      - 最高质量
      - 独特艺术风格
    pricing: 订阅制
```

### 3.2 异步生图流程

```
┌─────────────────────────────────────────────────────────────┐
│                    异步生图工作流                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 提交生图任务                                             │
│     ┌─────────┐     ┌─────────┐                            │
│     │ 金句 1  │────▶│ Task ID │                            │
│     │ 金句 2  │────▶│ Task ID │  → 记录任务 ID              │
│     │ 金句 3  │────▶│ Task ID │                            │
│     └─────────┘     └─────────┘                            │
│                                                             │
│  2. 继续处理其他任务（非阻塞）                                │
│     ┌─────────────────────────────────────┐                │
│     │  模板渲染  │  格式调整  │  预览生成   │                │
│     └─────────────────────────────────────┘                │
│                                                             │
│  3. 轮询检查生图状态                                         │
│     ┌─────────────────────────────────────┐                │
│     │  pending → processing → completed   │                │
│     └─────────────────────────────────────┘                │
│                                                             │
│  4. 图片就绪后插入文章                                       │
│     ┌─────────────────────────────────────┐                │
│     │  下载图片 → 上传 CDN → 替换占位符    │                │
│     └─────────────────────────────────────┘                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 Skill 调用示例

```javascript
// 调用即梦生图
async function generateImage(prompt, style) {
  const task = await mcp__jimeng__generate_image({
    prompt: prompt,
    style: style,
    size: '1024x576',
    async: true  // 异步模式
  });

  return {
    taskId: task.id,
    placeholder: `![生成中...](placeholder:${task.id})`,
    status: 'pending'
  };
}

// 检查并替换
async function replaceWithGeneratedImage(article, taskId) {
  const result = await mcp__jimeng__check_task({ taskId });

  if (result.status === 'completed') {
    const imageUrl = await uploadToCDN(result.imageUrl);
    return article.replace(
      `![生成中...](placeholder:${taskId})`,
      `![AI生成配图](${imageUrl})`
    );
  }

  return article; // 未完成，保持占位符
}
```

---

## 第四部分：二次加工模板系统

### 4.1 模板配置结构

```yaml
templates:
  minimal_white:
    name: 简约白
    theme:
      primary_color: '#ffffff'
      accent_color: '#333333'
      background: 'linear-gradient(180deg, #fff 0%, #f5f5f5 100%)'
    typography:
      heading_font: 'PingFang SC'
      body_font: 'Noto Sans SC'
      code_font: 'JetBrains Mono'
    elements:
      border_style: 'none'
      quote_style: 'left-border'
      code_theme: 'github-light'

  tech_blue:
    name: 科技蓝
    theme:
      primary_color: '#1a73e8'
      accent_color: '#4285f4'
      background: 'linear-gradient(135deg, #1a1a2e 0%, #16213e 100%)'
    typography:
      heading_font: 'SF Pro Display'
      body_font: 'Inter'
      code_font: 'Fira Code'
    elements:
      border_style: 'glow'
      quote_style: 'card'
      code_theme: 'one-dark'

  warm_orange:
    name: 暖心橙
    theme:
      primary_color: '#ff6b35'
      accent_color: '#f7c59f'
      background: 'linear-gradient(180deg, #fff5eb 0%, #ffe4d6 100%)'
    typography:
      heading_font: 'Noto Serif SC'
      body_font: 'Noto Sans SC'
    elements:
      border_style: 'rounded'
      quote_style: 'bubble'

  professional_black:
    name: 专业黑
    theme:
      primary_color: '#000000'
      accent_color: '#666666'
      background: '#121212'
    typography:
      heading_font: 'Roboto'
      body_font: 'Source Sans Pro'
    elements:
      border_style: 'sharp'
      quote_style: 'highlight'
      code_theme: 'dracula'
```

### 4.2 交互式选择器

```
┌─────────────────────────────────────────────────────────────┐
│                    📝 二次加工配置器                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🎨 选择主题风格                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 简约白  │  │ 科技蓝  │  │ 暖心橙  │  │ 专业黑  │        │
│  │   ○    │  │   ●    │  │   ○    │  │   ○    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                             │
│  🖼️ 边框样式                                                │
│  [ ] 无边框  [✓] 圆角  [ ] 阴影  [ ] 发光                    │
│                                                             │
│  📐 图片布局                                                 │
│  ○ 全宽  ● 居中  ○ 左对齐  ○ 卡片式                          │
│                                                             │
│  ✏️ 字体选择                                                 │
│  标题: [PingFang SC    ▼]                                   │
│  正文: [Noto Sans SC   ▼]                                   │
│  代码: [JetBrains Mono ▼]                                   │
│                                                             │
│  🎯 金句样式                                                 │
│  ○ 左边框  ● 卡片式  ○ 高亮背景  ○ 气泡框                    │
│                                                             │
│  ┌─────────────────────────────────────────────────┐        │
│  │                   [预览效果]                     │        │
│  └─────────────────────────────────────────────────┘        │
│                                                             │
│  [重置默认]              [保存配置]        [应用并继续]       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 样式应用示例

```javascript
function applyTemplate(article, template) {
  const config = templates[template];

  return {
    content: article,
    styles: {
      // CSS 变量
      '--primary-color': config.theme.primary_color,
      '--accent-color': config.theme.accent_color,
      '--bg-gradient': config.theme.background,

      // 字体
      '--font-heading': config.typography.heading_font,
      '--font-body': config.typography.body_font,
      '--font-code': config.typography.code_font,

      // 元素样式
      '--border-style': config.elements.border_style,
      '--quote-style': config.elements.quote_style
    },
    codeTheme: config.elements.code_theme
  };
}
```

---

## 第五部分：发布平台选择

### 5.1 支持的平台

| 平台 | 状态 | 特殊要求 | 对应 Skill |
|------|------|----------|------------|
| 知乎专栏 | ✅ 已支持 | 原生 Markdown | `zhihu-publisher` |
| 掘金 | ✅ 已支持 | 需选分类/标签 | `juejin-publisher` |
| 微信公众号 | ✅ 已支持 | 需至少1张图 | `wechat-publisher` |
| 小红书 | 🔜 规划中 | 图文并重 | - |
| 抖音图文 | 🔜 规划中 | 竖版图片 | - |
| Twitter/X | 🔜 规划中 | 140字限制 | - |
| LinkedIn | 🔜 规划中 | 专业调性 | - |

### 5.2 平台适配转换

```yaml
platform_adapters:
  zhihu:
    max_title_length: 100
    image_required: false
    markdown_support: true
    auto_convert:
      - code_blocks: syntax_highlight
      - tables: native_support

  juejin:
    max_title_length: 80
    image_required: false
    markdown_support: true
    required_fields:
      - category
      - tags (1-3)
    auto_convert:
      - code_blocks: codemirror

  wechat:
    max_title_length: 64
    image_required: true
    markdown_support: false
    auto_convert:
      - markdown: rich_text
      - code_blocks: image_screenshot
      - tables: image_render
    required_fields:
      - cover_image
      - digest (摘要)
```

### 5.3 一键多平台发布

```javascript
async function publishToMultiplePlatforms(article, platforms) {
  const results = {};

  for (const platform of platforms) {
    // 1. 平台适配转换
    const adaptedContent = await adaptForPlatform(article, platform);

    // 2. 调用对应 Skill
    const skill = getPublisherSkill(platform);

    // 3. 执行发布
    try {
      const result = await skill.publish(adaptedContent);
      results[platform] = {
        status: 'success',
        url: result.articleUrl
      };
    } catch (error) {
      results[platform] = {
        status: 'failed',
        error: error.message
      };
    }
  }

  return results;
}
```

---

## 第六部分：完整工作流演示

### 6.1 命令示例

```bash
# 完整流水线
"将 015 文档生成 3 种风格文案，配上 AI 图，发布到知乎和掘金"

# 分步执行
"生成技术型文案"
"提取金句并生成配图提示词"
"使用即梦生成配图"
"选择科技蓝主题"
"发布到知乎"
```

### 6.2 流程日志示例

```
📄 读取源文档: 015-zhihu-auto-publish-mcp-playwright.md
   ✓ 标题: 知乎专栏自动发布：MCP Playwright 自动化 SOP
   ✓ 字数: 3,245 字

🔄 Stage 1: 并行生成 3 种风格
   ├─ Agent 1 (技术型): 运行中...
   ├─ Agent 2 (故事型): 运行中...
   └─ Agent 3 (产品型): 运行中...

   ✓ 技术型完成 (2,890 字, 5 个代码块)
   ✓ 故事型完成 (1,650 字, 3 个场景)
   ✓ 产品型完成 (1,200 字, 4 个卖点)

💡 Stage 2: 金句提取
   ├─ 金句 1: "一键发布，告别繁琐的复制粘贴"
   ├─ 金句 2: "会话持久化，登录一次，长期有效"
   └─ 金句 3: "让 AI 成为你的发布助手"

   ✓ 生成 3 个图片提示词

🎨 Stage 3: AI 生图 (异步)
   ├─ 任务 1: task_abc123 (pending)
   ├─ 任务 2: task_def456 (pending)
   └─ 任务 3: task_ghi789 (pending)

   ... 继续其他处理 ...

   ✓ 所有图片生成完成，已插入文章

📝 Stage 4: 模板选择
   ? 请选择主题风格: 科技蓝
   ? 请选择边框样式: 圆角 + 阴影
   ? 请选择金句样式: 卡片式

   ✓ 模板应用完成

🚀 Stage 5: 发布
   ? 请选择发布平台: [✓] 知乎  [✓] 掘金  [ ] 微信

   ├─ 知乎: 发布成功 → https://zhuanlan.zhihu.com/p/xxxxx
   └─ 掘金: 发布成功 → https://juejin.cn/post/xxxxx

✅ 流水线完成！
   生成文章: 3 篇
   生成图片: 3 张
   发布平台: 2 个
   总耗时: 45 秒
```

---

## 常见问题

### Q1: 如何自定义风格模板？

**解决**：在 `templates` 配置中添加新的模板定义，包含主题色、字体、边框等配置。

### Q2: 生图失败如何处理？

**解决**：
- 检查 API 配额
- 简化提示词
- 切换备用生图引擎

### Q3: 多平台发布内容不一致？

**解决**：每个平台的适配器会自动处理格式差异，如微信不支持 Markdown 会自动转换为富文本。

### Q4: 如何保存常用配置？

**解决**：使用 `保存配置` 功能，下次可直接选择预设配置。

---

## 参考资源

- [015 知乎自动发布 SOP](./015-zhihu-auto-publish-mcp-playwright.md)
- [014 微信公众号自动发布 SOP](./014-wechat-mp-auto-publish-mcp-playwright.md)
- [013 掘金自动发布 SOP](./013-juejin-auto-publish-mcp-playwright.md)
- [Claude Code Skills 指南](https://docs.anthropic.com/claude-code/skills)
