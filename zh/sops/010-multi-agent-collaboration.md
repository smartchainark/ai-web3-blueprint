# 本地 AI 军团：免费调用三大模型并行思考

> 零成本、无限速、本地调用 Gemini + Codex + Claude 三大 AI 并行协作。

**文档编号**: 010
**日期**: 2025-12-29
**标签**: `Multi-Agent` `免费` `本地CLI` `并行` `头脑风暴`
**系列**: 本地 AI CLI 系列 (3/3)

---

## 为什么选择本地 AI 军团？

| 特性 | 本地 CLI 方案 | 传统 API 方案 |
|------|--------------|--------------|
| **成本** | 完全免费 | 按 token 计费 |
| **速度** | 原生速度，无网络延迟 | API 调用延迟 |
| **速率限制** | 无限制 | 受 rate limit 约束 |
| **隐私** | 代码不经第三方服务器 | 数据经过 API 服务器 |
| **离线** | Codex 支持离线 | 必须联网 |

## 概述

本 SOP 介绍如何在 Claude Code 中构建 Multi-Agent 协作系统：

- **零成本**：使用本地 CLI，Gemini/Codex 完全免费
- **原生速度**：直接调用 CLI，无 API 延迟
- **三大 AI 并行**：Claude(分析) + Gemini(创意) + Codex(工程)
- **多场景支持**：头脑风暴、方案评审、辩论等

> 📚 **系列导航**
> - [005 - Gemini CLI 多模态处理](./005-gemini-cli-multimodal-integration.md)
> - [009 - Codex CLI 代码生成](./009-codex-cli-integration.md)
> - **010 - 本地 AI 军团（当前）**

## 系统架构

```
用户提问
    ↓
Claude (调度者)
    ↓
┌───────────────┬───────────────┬───────────────┐
│   Claude      │    Gemini     │    Codex      │
│  (分析者)     │   (创意者)    │   (工程师)    │
└───────────────┴───────────────┴───────────────┘
    ↓               ↓               ↓
    └───────────────┴───────────────┘
                    ↓
            Claude (综合者)
                    ↓
              结构化输出
```

## Agent 角色定义

| Agent | 角色 | 视角特点 | 独特价值 |
|-------|------|---------|---------|
| **Claude** | 分析者 | 逻辑严谨、安全意识强 | 风险识别、边界情况 |
| **Gemini** | 创意者 | 发散思维、多模态视角 | 创新方案、跨领域联想 |
| **Codex** | 工程师 | 代码导向、实现细节 | 技术可行性、代码示例 |

## 使用场景

### 场景 1：并行问答

```
/collab {问题}
```

三个 Agent 独立回答同一问题，汇总展示。

**适用**：技术决策、方案选型、开放性问题

### 场景 2：头脑风暴

```
/brainstorm {话题}
```

强调创意发散，不评判任何想法。

**规则**：
- 数量优先于质量
- 鼓励疯狂的想法
- 可以组合和改进他人想法

### 场景 3：方案评审

```
/review {方案}
```

多角度审查方案，找漏洞和风险。

**角色调整**：
- Claude → 安全审查员
- Gemini → 用户体验审查
- Codex → 技术可行性审查

### 场景 4：辩论模式

```
/debate {观点}
```

正反方观点对比，中立裁判总结。

**立场分配**：
- Claude → 支持方
- Gemini → 反对方
- Codex → 中立裁判

## 实现原理

### 步骤 1：解析问题

识别用户意图，确定讨论模式：

```
用户：让三个 AI 讨论一下微服务架构的利弊
        ↓
模式：辩论模式 (/debate)
问题：微服务架构的利弊
```

### 步骤 2：并行调用

使用 Task 工具**同时**启动三个子代理：

```yaml
# 关键：在同一条消息中发起三个 Task 调用

Task 1 (Claude):
  subagent_type: general-purpose
  prompt: 作为分析者角色分析问题...

Task 2 (Gemini):
  subagent_type: general-purpose
  prompt: 执行 gemini "作为创意者..." -y

Task 3 (Codex):
  subagent_type: general-purpose
  prompt: 执行 codex "作为工程师..."
```

### 步骤 3：汇总输出

收集三个 Agent 结果，结构化展示：

```markdown
## 问题：{用户问题}

### 🔍 Claude (分析者)
{回答内容}

### 💡 Gemini (创意者)
{回答内容}

### 🔧 Codex (工程师)
{回答内容}

## 📊 观点汇总
| 视角 | 核心观点 |
|------|---------|
| ... | ... |

## ✅ 共识点
- ...

## ⚠️ 分歧点
- ...

## 🎯 建议行动
1. ...
```

## 成本控制

多 Agent 并行 = 多倍 API 调用成本。

| 模式 | Agent 数量 | 成本 | 适用场景 |
|------|-----------|------|---------|
| 快速 | 1 (Claude) | 1x | 简单问题 |
| 标准 | 2 (Claude + Gemini) | 2x | 一般讨论 |
| 深度 | 3 (全部) | 3x | 重要决策 |

**默认**：标准模式

**指定方式**：
- "只要 Claude" → 快速模式
- "三个都要" / "深度分析" → 深度模式

## 输出格式

### Token 限制

每个 Agent 回复限制在 500 token 以内，避免信息过载。

### 结构化模板

```markdown
## 问题：{原始问题}

---

### 🔍 Claude (分析者)

**核心观点**：...

**关键考虑**：
- ...
- ...

---

### 💡 Gemini (创意者)

**核心观点**：...

**创新想法**：
- ...
- ...

---

### 🔧 Codex (工程师)

**核心观点**：...

**实现建议**：
```code
// 示例代码
```

---

## 📊 汇总分析

### 共识
...

### 分歧
...

### 建议
...
```

## 错误处理

| 情况 | 处理方式 |
|------|---------|
| 单个 Agent 超时 (>60s) | 跳过该 Agent，标注"未响应" |
| 单个 Agent 报错 | 显示错误信息，继续处理其他 |
| 全部失败 | 回退到 Claude 单独回答 |

## 注意事项

1. **并行执行**：三个 Task 必须在同一消息中发起
2. **敏感信息**：不要将密钥、密码传递给 Gemini/Codex
3. **上下文隔离**：每个 Agent 在独立子代理中运行
4. **费用控制**：重要问题才使用深度模式

## 依赖技能

确保以下技能已启用：

- `gemini-cli` - Gemini CLI 多模态助手
- `codex-cli` - Codex CLI 编程助手

## 示例对话

**用户**：
```
/collab 我想做一个 AI 驱动的代码审查工具，有什么建议？
```

**输出**：
```
## 问题：AI 驱动的代码审查工具建议

---

### 🔍 Claude (分析者)

核心挑战是**准确性与误报率的平衡**。

关键考虑：
- 需要处理多语言支持
- 企业场景需要本地部署选项
- 与现有 CI/CD 流程集成

潜在风险：
- LLM 可能产生错误的修复建议
- 大型代码库的性能问题

---

### 💡 Gemini (创意者)

可以引入「代码气味地图」概念，将问题可视化。

创新想法：
- 结合 Git 历史分析 bug 热点区域
- 用知识图谱关联相似问题的历史修复
- 支持语音/截图输入问题描述

---

### 🔧 Codex (工程师)

技术栈建议：tree-sitter + LLM

实现架构：
```python
class CodeReviewer:
    def __init__(self):
        self.parser = TreeSitterParser()
        self.llm = LLMClient()

    def review(self, code: str) -> List[Issue]:
        ast = self.parser.parse(code)
        context = self.extract_context(ast)
        return self.llm.analyze(context)
```

---

## 📊 汇总

### ✅ 共识
- 需要多语言支持
- 需要与 CI/CD 集成
- LLM 输出需要人工确认

### ⚠️ 分歧
- 架构：Claude 建议谨慎，Gemini 建议大胆创新

### 🎯 建议
1. 先做单语言 MVP 验证
2. 用 tree-sitter 做基础解析
3. 设计人工确认流程
```

## 多轮迭代讨论

支持 Agent 之间多轮互动，互相回应、质疑、补充。

### 触发方式

```
/collab --rounds=3 {问题}     # 指定 3 轮讨论
/debate --deep {观点}         # 自动启用多轮
```

### 工作流程

```
第一轮：各自独立回答
    ↓
（摘要压缩）
    ↓
第二轮：看到其他人观点后，补充或反驳
    ↓
（摘要压缩）
    ↓
第三轮：形成共识或明确保留分歧
    ↓
最终汇总
```

### 上下文传递（摘要压缩）

每轮结束后，Claude 调度者对结果进行摘要，下一轮只传递摘要：

```
第 N+1 轮 prompt 模板：

【历史摘要】
第 1 轮要点：
- 分析者：{Claude 核心观点，50字}
- 创意者：{Gemini 核心观点，50字}
- 工程师：{Codex 核心观点，50字}

第 2 轮要点：
- ...

【本轮任务】
请针对以上观点进行补充或反驳...

问题：{原始问题}
```

### 摘要压缩实现

每轮收集结果后，调用 Claude 生成摘要：

```yaml
Task: 生成轮次摘要
  subagent_type: general-purpose
  model: haiku  # 用便宜的模型做摘要
  prompt: |
    请将以下三个观点各压缩为一句话（不超过50字）：

    分析者原文：{claude_response}
    创意者原文：{gemini_response}
    工程师原文：{codex_response}

    输出格式：
    - 分析者：...
    - 创意者：...
    - 工程师：...
```

### 多轮输出格式

```markdown
## 问题：{用户问题}

---

### 第一轮：独立回答

#### 🔍 Claude (分析者)
{详细回答}

#### 💡 Gemini (创意者)
{详细回答}

#### 🔧 Codex (工程师)
{详细回答}

---

### 第二轮：互动讨论

#### 🔍 Claude (分析者)
> 针对 Gemini 的观点：...
{补充/反驳}

#### 💡 Gemini (创意者)
> 针对 Codex 的观点：...
{补充/反驳}

#### 🔧 Codex (工程师)
> 针对 Claude 的观点：...
{补充/反驳}

---

### 第三轮：达成共识

{Claude 调度者总结}

---

## 📊 最终汇总

### ✅ 共识点
- ...

### ⚠️ 保留分歧
- ...

### 🎯 建议行动
1. ...
```

### 轮次控制参数

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--rounds=N` | 讨论轮数 | 1 |
| `--max-rounds` | 最大轮数上限 | 5 |
| `--auto-stop` | 达成共识后自动停止 | true |

## 技能开发

本 SOP 已开发为 Claude Code 技能，可直接在对话中触发。

### 技能文件结构

```
library/multi-agent-collab/
└── SKILL.md              # 技能定义文件
```

### 技能配置

```yaml
---
name: multi-agent-collab
description: Multi-Agent 协作系统，由 Claude 调度，Gemini/Codex/Claude 并行回答同一问题。
             当用户说"多Agent讨论"、"头脑风暴"、"集思广益"、"三个AI一起"时触发。
---
```

### 触发词

| 用户说 | 触发 |
|--------|------|
| "让多个 AI 一起讨论..." | ✅ |
| "头脑风暴一下..." | ✅ |
| "集思广益..." | ✅ |
| "三个 Agent 分析..." | ✅ |
| "/collab {问题}" | ✅ |
| "/brainstorm {话题}" | ✅ |

### 安装技能

```bash
# 复制技能到 Claude Code 技能库
cp -r library/multi-agent-collab ~/.claude/skills/

# 或使用符号链接（推荐，便于更新）
ln -s /path/to/library/multi-agent-collab ~/.claude/skills/multi-agent-collab
```

### 依赖技能

使用前需确保以下技能已安装：

- `gemini-cli` - Gemini CLI 多模态助手
- `codex-cli` - Codex CLI 编程助手

## 参考资源

- [Multi-Agent Systems 概述](https://en.wikipedia.org/wiki/Multi-agent_system)
- [Claude Code Skills 文档](https://docs.anthropic.com/claude-code/skills)
- [Gemini CLI 文档](https://github.com/google-gemini/gemini-cli)
- [Codex CLI 文档](https://github.com/openai/codex)
