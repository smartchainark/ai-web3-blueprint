# Claude 技能创建规范化 SOP

> 让 Claude 在创建技能时自动遵循你的技能库规范，避免污染现有结构

**文档编号**: 006
**日期**: 2025-12-29
**标签**: `Claude Code` `Skills` `SOP` `自动化`

---

## 概述

当你让 Claude 帮你创建新技能时，如果没有明确规范，Claude 可能会：
- 直接在 `~/.claude/skills/` 创建目录（绕过你的技能库）
- 在 `enabled/` 创建实际目录（而非符号链接）
- 忘记更新 README 索引

本文档介绍如何通过 **skill-creator 技能** 让 Claude 自动：
1. 检测用户是否有自定义技能库
2. 按规范创建到正确位置
3. 用符号链接启用
4. 更新技能索引

---

## 第一部分：问题场景

### 1.1 常见错误

| 错误操作 | 问题 |
|---------|------|
| `mkdir ~/.claude/skills/new-skill` | 绕过技能库，无法 Git 管理 |
| `mkdir ~/Projects/my_skils/enabled/new-skill` | 直接在 enabled 创建实际目录 |
| `cp -r library/skill enabled/` | 复制文件而非符号链接 |
| 忘记更新 README | 技能列表不完整 |

### 1.2 正确做法

```
用户说：帮我创建一个 xxx 技能
    ↓
Claude 检查：用户有技能库吗？
    ↓
有 → 读取 README 了解规范
    ↓
创建到 library/
    ↓
符号链接到 enabled/
    ↓
更新 README 索引
```

---

## 第二部分：解决方案

### 2.1 核心思路

将创建规范写入 `skill-creator` 技能中，Claude 在创建新技能时会自动读取并遵循。

### 2.2 技能库检测逻辑

在 `skill-creator/SKILL.md` 中添加：

```markdown
## 用户技能库规范（必读）

> **重要**：创建技能前，必须先检查用户是否有自定义技能库。

### Step 0: 检查用户技能库

在创建任何技能之前，**必须**执行：

\`\`\`bash
# 检查用户是否有自定义技能库
ls ~/Projects/my_skils/README.md 2>/dev/null && echo "✅ 已有技能库" || echo "❌ 无技能库"
\`\`\`
```

### 2.3 两种情况处理

**情况 A：用户已有技能库**

```markdown
### 情况 A: 用户已有技能库

如果 `~/Projects/my_skils/` 存在，必须遵循其规范：

1. **先读 README** — `cat ~/Projects/my_skils/README.md`
2. **创建到 `library/`** — 不要在 `~/.claude/skills/` 创建
3. **用符号链接启用** — 不要复制文件
4. **更新 README** — 添加新技能到技能列表

\`\`\`bash
# 正确做法
mkdir -p ~/Projects/my_skils/library/new-skill
# 编写 SKILL.md
cd ~/Projects/my_skils/enabled && ln -s ../library/new-skill .
# 更新 README 技能列表
\`\`\`
```

**情况 B：用户没有技能库（首次使用）**

```markdown
### 情况 B: 用户没有技能库（首次使用）

**推荐方案**：建议用户使用 Git 管理的技能库。

向用户介绍：

> 💡 **推荐使用 Git 管理技能库**
>
> 这种方式可以让你的技能：
> - **跨平台复用** — macOS / Linux / Windows 一套技能
> - **多设备同步** — Git 管理，新设备一键部署
> - **灵活启用** — 按需启用技能，保持环境整洁
> - **版本迭代** — 技能持续优化，历史可追溯
>
> 是否要我帮你初始化一个技能库？

如果用户同意，执行初始化脚本创建完整的技能库结构。
```

---

## 第三部分：实现步骤

### 3.1 更新 skill-creator 技能

将规范添加到 `~/Projects/my_skils/library/skill-creator/SKILL.md`：

```bash
# 编辑 skill-creator 技能
vim ~/Projects/my_skils/library/skill-creator/SKILL.md
```

添加"用户技能库规范（必读）"章节（见上文 2.2-2.3）。

### 3.2 更新技能库 README

在 `~/Projects/my_skils/README.md` 末尾添加：

```markdown
## Claude 新建技能规范

> **重要**：当 Claude 被要求新建技能时，必须遵循以下流程。

### 必读流程

1. **先读本 README** — 了解现有技能分类和命名规范
2. **检查是否已存在** — `ls ~/Projects/my_skils/library/` 避免重复创建
3. **创建到 `library/`** — 不要直接在 `enabled/` 或 `~/.claude/skills/` 创建
4. **按需启用** — 用符号链接启用，不直接复制文件

### 正确做法

\`\`\`bash
# 1. 创建技能目录
mkdir -p ~/Projects/my_skils/library/new-skill

# 2. 创建 SKILL.md
# (编写技能内容)

# 3. 启用（创建符号链接）
cd ~/Projects/my_skils/enabled && ln -s ../library/new-skill .

# 4. 更新本 README 的技能列表
\`\`\`

### 错误做法

\`\`\`bash
# ❌ 直接在 ~/.claude/skills/ 创建
mkdir ~/.claude/skills/new-skill

# ❌ 直接在 enabled/ 创建实际目录
mkdir ~/Projects/my_skils/enabled/new-skill

# ❌ 复制文件而不是符号链接
cp -r library/skill enabled/skill
\`\`\`
```

### 3.3 提交更新

```bash
cd ~/Projects/my_skils
git add .
git commit -m "docs: add skill creation SOP to README and skill-creator"
git push
```

---

## 第四部分：验证测试

### 4.1 测试场景

在新会话中对 Claude 说：

> 帮我创建一个 `code-review` 技能，用于代码审查

### 4.2 预期行为

Claude 应该：

1. ✅ 先检查 `~/Projects/my_skils/README.md` 是否存在
2. ✅ 读取 README 了解现有技能和规范
3. ✅ 在 `library/code-review/` 创建目录
4. ✅ 编写 `SKILL.md`
5. ✅ 在 `enabled/` 创建符号链接
6. ✅ 更新 README 技能列表

### 4.3 验证命令

```bash
# 检查技能位置
ls ~/Projects/my_skils/library/code-review/SKILL.md

# 检查符号链接
ls -la ~/Projects/my_skils/enabled/code-review
# 应显示: code-review -> ../library/code-review

# 检查 README 是否更新
grep "code-review" ~/Projects/my_skils/README.md
```

---

## 第五部分：首次用户引导

对于没有技能库的新用户，skill-creator 会提示：

```
💡 推荐使用 Git 管理技能库

这种方式可以让你的技能：
- 跨平台复用 — macOS / Linux / Windows 一套技能
- 多设备同步 — Git 管理，新设备一键部署
- 灵活启用 — 按需启用技能，保持环境整洁
- 版本迭代 — 技能持续优化，历史可追溯

是否要我帮你初始化一个技能库？
```

如果用户同意，Claude 会执行初始化脚本：

```bash
# 1. 创建技能库目录结构
mkdir -p ~/Projects/my_skils/{library,enabled}

# 2. 创建 README（包含完整规范）
cat > ~/Projects/my_skils/README.md << 'EOF'
# Claude Skills Library
...
EOF

# 3. 创建符号链接
ln -sf ~/Projects/my_skils/enabled ~/.claude/skills

# 4. 初始化 Git（可选）
cd ~/Projects/my_skils && git init
```

---

## 常见问题

### Q1: Claude 还是在错误位置创建了技能

**原因**：skill-creator 技能未启用，或规范未正确添加。

**解决**：
```bash
# 确保 skill-creator 已启用
ls ~/Projects/my_skils/enabled/skill-creator

# 如未启用
cd ~/Projects/my_skils/enabled && ln -s ../library/skill-creator .

# 检查规范是否存在
grep "用户技能库规范" ~/Projects/my_skils/library/skill-creator/SKILL.md
```

### Q2: 新会话中规范不生效

**原因**：Claude 在新会话中可能不会自动读取 skill-creator。

**解决**：在请求创建技能时，明确提到"使用 skill-creator"或"/skill-creator"。

### Q3: 如何让规范在所有会话中自动生效

**方案**：将核心规范也放入 README.md，Claude 在创建技能前会读取项目的 README。

---

## 最佳实践

### Do's

- ✅ 保持 skill-creator 技能始终启用
- ✅ 定期检查 README 技能列表是否完整
- ✅ 新技能创建后立即 `git commit`
- ✅ 在 skill-creator 中维护最新规范

### Don'ts

- ❌ 不要在多个位置维护相同规范（容易不一致）
- ❌ 不要忽略 README 更新
- ❌ 不要在不同设备使用不同的规范

---

## 技能库结构参考

```
my_skils/
├── README.md                 # 技能索引 + 创建规范
├── library/                  # 全部技能（实际文件）
│   ├── skill-creator/        # 技能创建器（含规范）
│   │   └── SKILL.md
│   ├── task-manager/
│   ├── gemini-cli/
│   └── ...
└── enabled/                  # 已启用（符号链接）
    ├── skill-creator -> ../library/skill-creator
    ├── task-manager -> ../library/task-manager
    └── ...
```

---

## 参考资源

- [004 - Claude Skills 跨平台部署与管理](./004-claude-skills-setup.md)
- [Claude Code Skills 文档](https://docs.anthropic.com/claude-code/skills)
- [技能库 GitHub](https://github.com/smartchainark/my_skils)
